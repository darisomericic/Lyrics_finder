# 🎶 Lyrics finder with React 🎶

## 🎶 About lyrics finder 🎶

Its an web application that lets users search for song lyrics instantly. Simply enter an artist and a song title, and the app fetches the lyrics in real-time. 


## Available Scripts

In the project directory, you can run:

### `npm start`

Runs the app in the development mode.\
Open [http://localhost:3000](http://localhost:3000) to view it in your browser.

The page will reload when you make changes.\
You may also see any lint errors in the console.

### `npm start -- --host X.X.X.X`

This command starts the React development server and makes the app available on your local network.
By replacing `X.X.X.X` with your computer’s local IP address, other devices on the same Wi-Fi network (like phones, tablets, or other PCs) can open your app in their browser.


### `npm test`

Launches the test runner in the interactive watch mode.

### `npm run build`

Builds the app for production to the `build` folder.\
It correctly bundles React in production mode and optimizes the build for the best performance.

The build is minified and the filenames include the hashes.\
Your app is ready to be deployed!

----------------------------------------------------

### 🎶 What have i learned making this project? 🎶
----------

- Putting API in React App using Axios

- Better file structure

- Learned that React is very useful for bigger projects 

- Learned what React is 

---------------------------------------------------------
### 🎶 What i think was difficult 🎶
---

- Learning a new programming language, with barely enough skills from JS.

- Meeting the API for the first time, and putting it in my code

---
### What i think could be better with this interest group
---
- I think it would've been better if we got to see live coding a little bit more from teachers, just to make sure that we understood the assignment

- Besides that i think the interest group was really good and i learned some new things here